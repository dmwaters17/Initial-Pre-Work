// The ternary operator functions as a single line if/else statement and can be used to compare values to return true or false. 

function checkEqual(a, b) {
  return (a == b ? true : false);
}

checkEqual(1, 2);
