// Comparison operators use boolean logic to return certain values given the corrrect conditions. The equals operator will return true if two values match and false if they do not match. 

// Setup
function testEqual(val) {
  if (val == 12) {
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(12);
