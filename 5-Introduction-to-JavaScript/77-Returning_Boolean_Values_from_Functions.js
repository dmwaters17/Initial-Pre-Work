// Comparison operators use boolean logic, therefore if / else statements with true or false returns are not necessary.  Simply comparing the values will return a true or false.

function isLess(a, b) {
  // Fix this code
   return a < b;
}

// Change these values to test
isLess(10, 15);
