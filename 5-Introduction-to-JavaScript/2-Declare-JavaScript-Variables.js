/* Javascript has seven data types. Variables allow computers to point to the label of the data as opposed to the data itself. Variables can store different data at different times. Variables are defined with 'var'. Variables can contain numbers, letters, and special characters but may not start with a number or contain spaces. */
// Example
var ourName;

// Declare myName below this line

var myName;
