// Strings are immutable and cannot be changed. The variable must be reassigned a new string in order to alter the original string value of that variable.

// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me
