// The inequality operator will return true when values do not equal each other and false when they do. It is the opposite of the equality operator.

// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);
